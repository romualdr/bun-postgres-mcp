{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "bun",
      "request": "launch",
      "name": "Debug index.ts",
      "program": "${workspaceFolder}/index.ts",
      "args": [
        "postgresql://user:password@localhost/dbname",
        "--anonymize"
      ],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "watchMode": false
    }
  ]
}
